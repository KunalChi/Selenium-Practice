package com.Arrays;

public class Encapsulation {

	//public static void main(String[] args) {
		//This method is declared as public meaning that any class that can use MyFirstTest can call the method.
		public void account (){
		
	}

}
/*Encapsulation
	 * consists of data protection. Protection of data prevents direct access from other parts of the program. 
	 * Access modifiers place limitations on classes, variables, and methods while accessor methods set and return 
	 * specific data.
	 */
/* Classes 
 	 * define data and code that operates on the data. The data is represented by variables while the
	 * code is represented by methods.
	 */
/*	Syntax 
 * class ClassName 
 * { 
 * // Declare Instance Variable 
 * variableType variableName1; 
 * variableType variableName2; 
 * variableType variableNameN; 
 * // Declare Methods 
 * methodType methodName1 () 
 * { 
 * // Body of the Method } 
	methodType methodName2 () { 
	} 
	methodType methodNameN () { 
	// Body of the Method 
	} 
	}
*/	